# zlib licence

oscpm is released under the zlib licence, chosen for its permissiveness and
because it does not require attribution in binary distributions, which suits
a header-only library that gets compiled into plugins and firmware. Changing
licence after adoption would require consent from every contributor, so this
was settled before the first release.
